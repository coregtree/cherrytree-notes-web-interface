# cherrytree-notes-web-interface
A web interface upgrade for cherrytree notes provides a significant upgrade to the default website interface given by Export as HTML and remains fully client side. It provides full instant image and text search across all pages (through cache files), makes the interface mobile friendly, adds a dark theme, and an font size resizing feature.
